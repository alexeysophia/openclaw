{
  "id": "orchestrator-channel",
  "name": "Orchestrator channel",
  "description": "Delivers assistant messages to the persond orchestrator (outbound-only channel).",
  "activation": {
    "onStartup": true
  },
  "channels": ["orchestrator"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "channelConfigs": {
    "orchestrator": {
      "schema": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": { "type": "boolean" }
        }
      }
    }
  }
}
